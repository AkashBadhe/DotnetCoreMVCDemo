@model  IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "View";
}

<h1>Roles</h1>

<div>
    @if (Model.Any())
    {
        foreach (var role in Model)
        {
            <div class="card">
                <div class="card-header">
                    RoleId: @role.Id
                </div>
                <div class="card-body">
                    <h5 class="card-title">@role.Name</h5>
                    <a asp-action="Edit" asp-controller="Administration" class="btn btn-primary" />
                    <a asp-action="Delete" asp-controller="Administration" asp-route-id="@role.Id" class="btn btn-danger" />
                </div>
            </div>
        }
    }
    else
    {
        <div class="card">
            <div class="card-header">
                No Roles available. To create role click on create button.
            </div>
            <div class="card-body">
                <a asp-action="Create" asp-controller="Administration" class="btn btn-primary" />
            </div>
        </div>
    }
</div>
